version: '3.9'

services:
  db:
    build: ./db
    restart: always
    ports:
      - "33060:3306"
    environment:
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: world

  app:
    build: .
    depends_on:
      - db
    restart: on-failure

